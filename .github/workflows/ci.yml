name: CI

on:
  push:
    branches: [main]

jobs:
  build:
    uses: login-linjeforeningen-for-it/.github/.github/workflows/build-image.yml@main
    permissions:
      contents: read
      packages: write
